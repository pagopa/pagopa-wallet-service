package it.pagopa.wallet.config
/*
import it.pagopa.wallet.documents.wallets.UuidIdentifiedEntity
import java.util.*
import org.springframework.context.annotation.Bean
import org.springframework.data.mongodb.core.mapping.event.BeforeConvertCallback

@Bean
fun beforeSaveCallback(): BeforeConvertCallback<UuidIdentifiedEntity> {
    return BeforeConvertCallback<UuidIdentifiedEntity> {
        entity: UuidIdentifiedEntity,
        collection: String ->
        if (entity.id == null) {
            entity.id = UUID.randomUUID()
        }
        entity
    }
}
*/
